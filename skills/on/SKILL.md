---
description: Turn adactus on, so its Stop hook keeps Claude working past lazy pauses and premature completion claims.
disable-model-invocation: true
---
Run this command and report its output to the user in one line:

```sh
node "${CLAUDE_PLUGIN_ROOT}/bin/adactus.js" on
```
