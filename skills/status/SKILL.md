---
description: Show whether adactus is on and its most recent Stop hook decisions.
disable-model-invocation: true
---
Run this command and show its output to the user as-is:

```sh
node "${CLAUDE_PLUGIN_ROOT}/bin/adactus.js" status
```
