---
description: Turn adactus off, so its Stop hook lets every stop through until it is turned back on.
disable-model-invocation: true
---
Run this command and report its output to the user in one line:

```sh
node "${CLAUDE_PLUGIN_ROOT}/bin/adactus.js" off
```
